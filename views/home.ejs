<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>User Table</title>
<style>
    table {
        width: 100%;
        border-collapse: collapse;
    }
    th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
    }
    th {
        background-color: #f2f2f2;
    }
    .btn {
        padding: 5px 10px;
        margin-right: 5px;
        border: none;
        border-radius: 3px;
        cursor: pointer;
    }
    .btn-detail {
        background-color: #008CBA;
        color: white;
    }
    .btn-add {
        background-color: #008CBA;
        color: white;
        margin-top: 10px;
    }
</style>
</head>
<body>

<h1>Data Mahasiswa</h1>

<table>
    <thead>
        <tr>
            <th>No</th>
            <th>Name</th>
            <th>Age</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>

        <% for(let i=0; i<result.length; i++){ %>
            <tr>
                <td><%= i %></td>
                <td><%= result[i].nama %></td>
                <td><%= result[i].usia %></td>
                <td> <a href="/detail/<%= result[i].nama %>" class="btn btn-detail" style="text-decoration: none;">Detail</a> </td>
            </tr>
        <% } %>

    </tbody>
</table>

<a href="/add" class="btn btn-add" style="text-decoration: none;">Add</a>

</body>
</html>
